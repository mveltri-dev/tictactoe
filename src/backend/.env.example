# Configuration Azure PostgreSQL (format Microsoft)
DB_HOST=your-database.postgres.database.azure.com
DB_USER=your_admin_username
DB_NAME=postgres
DB_PASSWORD=your_password
DB_PORT=5432

# JWT Configuration (Generate a strong random secret for production!)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-key-at-least-32-characters-long-change-this-in-production
JWT_ISSUER=TicTacToeApi
JWT_AUDIENCE=TicTacToeClient
